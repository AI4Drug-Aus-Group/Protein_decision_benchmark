from __future__ import annotations
import csv
from pathlib import Path
ROOT = Path(__file__).resolve().parents[1]
OUT = ROOT / 'results' / 'external_wrappers' / 'competitor_wrapper_feasibility.csv'
ROWS = [{'framework': 'FSFP', 'current_status': 'official repo cloned; score-based approximation available; pipeline blocked in Python 3.12', 'current_outputs': 'results/low_n/low_n_multi_zscore_ridge_nongiant_*; results/external_wrappers/official_wrapper_smoke_status.csv', 'faithful_requirements': 'Python 3.10 environment matching README; torch 2.0.1/CUDA 11.8 stack; learn2learn==0.2.0; ProteinGym substitution inputs; optional SaProt structure sequences', 'main_risk': 'learn2learn==0.2.0 fails to build on Python 3.12 and fsfp.trainer imports learn2learn.MAML at module import time; precomputed zero-shot scores are not FSFP embeddings/adaptation', 'recommended_next_step': 'create a Python 3.10/conda environment for a faithful run, or use a lazy-import/shimmed wrapper and label it patched', 'priority': 'medium'}, {'framework': 'EVOLVEpro', 'current_status': 'official DMS loop smoke test passed with surrogate score-vector embedding', 'current_outputs': 'results/active_learning/active_learning_representative_metrics_seeds0_4.csv; results/external_wrappers/evolvepro_smoke/*', 'faithful_requirements': 'PLM embedding extractor/cache matching EVOLVEpro settings; official dms_main.py regression/acquisition recipe; iterative retraining under identical candidate pools', 'main_risk': 'the official code path runs, but the current smoke uses precomputed zero-shot score columns as a surrogate embedding rather than EVOLVEpro paper embeddings', 'recommended_next_step': 'generate or obtain real embeddings and rerun the already working official dms_main.py wrapper', 'priority': 'medium'}, {'framework': 'MULTI-evolve', 'current_status': 'official package imports; bundled example reaches experiment runner but stops at W&B sweep authentication', 'current_outputs': 'results/multi_evolve_style/pairwise_residual_*; results/multi_evolve_style/pairwise_plm_residual_*; results/external_wrappers/official_wrapper_smoke_status.csv', 'faithful_requirements': 'valid W&B account/key for official sweep code; exact architecture/code; single+double training protocol; higher-order target split matching the paper', 'main_risk': 'official p1_train.py creates a W&B sweep even in test-mode attempts; fake/offline keys fail before model results are written', 'recommended_next_step': 'run with a valid W&B key for faithful reproduction, or patch/monkeypatch W&B and label the result patched', 'priority': 'low_to_medium'}, {'framework': 'Hsu-style supervised evolutionary features', 'current_status': 'partially covered by MSA/evolution zero-shot scores plus ridge/XGBoost', 'current_outputs': 'results/low_n/low_n_all_supervised_combined_nongiant_percentile_summary.csv', 'faithful_requirements': 'feature extraction matching the original evolutionary/statistical features', 'main_risk': 'exact feature parity may be hard without reimplementing paper-specific preprocessing', 'recommended_next_step': 'treat as optional; current GEMME/EVmutation/TranceptEVE/S2F/S3F score features cover much of the evolutionary signal', 'priority': 'low'}]

def main() -> None:
    OUT.parent.mkdir(parents=True, exist_ok=True)
    with OUT.open('w', newline='', encoding='utf-8') as fh:
        writer = csv.DictWriter(fh, fieldnames=list(ROWS[0].keys()))
        writer.writeheader()
        writer.writerows(ROWS)
    print(f'wrote {OUT}')
if __name__ == '__main__':
    main()
